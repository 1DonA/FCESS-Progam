/**
 * Deprecated. CSV import is now an inline "Import CSV" button on each
 * Departments / Courses / Lecturers page. This file is kept as a stub so
 * any lingering imports don't break the type-check; the route is gone.
 */
export function ImportCSV() {
    return null;
}
